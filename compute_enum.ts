enum weekdays{
    Monday=1,
    Tues=Monday+1,
    wed=Tues+1,
    thurs=wed+1,
    fri=thurs+1,
    sat=fri+1,
    sun=sat+1
}
console.log(weekdays);
//string values=will execute only once
// number values will twice (as per assignmentand the reverse manner)